/**
 *テーブルの特定のセルのみ枠線を消す
 * http://hensa40.cutegirl.jp/archives/657
 */
var Note = {
		MakeObject: function(){
			//$(.NoteStart);
		}, //2つ目移行を定義するときはカンマでつなぐ
		SetRange: function(FromCol, FromRow, ToCol){
			alert('Heelo');
		}
};